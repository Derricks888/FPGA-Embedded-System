## Simulation (ModelSim)

From the `sim/` directory:

### GUI Simulation
```sh
vsim -do run.do
```

### Command-Line Simulation
```sh
vsim -c -do vsim.tcl
```

The waveform is automatically configured using `wave.do`.
